// De main functie is het begin van het programma
int main() {
    // Maak een getal aan en verdubbel het
    int x = 5;
    int y = doubler(x);

    // Het programma is succesvol afgerond
    return 0;
}

// Functie om een getal te verdubbelen
int doubler(int x) {
    // Geef het dubbele van het getal terug
    return x * 2;
}